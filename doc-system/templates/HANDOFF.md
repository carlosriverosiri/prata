# HANDOFF — <PROJECT> state of play

> **Role: TRANSIENT.** A self-contained brief to continue on another machine /
> fresh session with no memory of prior work. It goes stale by design — **delete or
> rewrite it once you have moved on**, so it never misleads.

<!-- freshness: valid-as-of <git-short-sha> (<tag>) <ISO-date> -->
<!-- A reader compares this SHA to HEAD: far behind ⇒ treat as stale. -->

## 0. Read first (the docs are the source of truth)

- `PROJECT-IDENTITY.md` · `MASTER.md` · `AGENTS.md` §2 (freshness rule).
- `DECISIONS-REJECTED.md` (don't re-tread these) · `CONSTANTS.md`.

## 1. State

- Branch / tag: <…>. Working tree: <clean? in flight?>. Open PRs: <…>.

## 2. Environment (build/test on a new machine)

- Toolchain: <…>. Verification gate: <…>.

## 3. Candidate next steps (none urgent unless noted)

- <…>

## 4. Delete me

This brief is transient — delete or rewrite it once you have moved past the state
above.
