# <PROJECT> — Master Document

> **Role: SOURCE. Hand-curated synthesis — not generated.** The project's collected
> truth in distilled form, kept current by hand in the same change as behavior
> changes (`AGENTS.md` §2). Deliberately *not* a concatenation of the other docs —
> the value is the synthesis. Carry conclusions + one-line rationale here; push the
> full reasoning to a Decision Record.

<!-- freshness: valid-as-of <git-short-sha> (<tag>) <ISO-date> -->

## What <PROJECT> is

<One tight paragraph: the problem it solves, for whom, and the single defining
design stance.>

## How it works (the main flows)

<Step-by-step of each primary user/operational flow. Where a step encodes a
non-obvious decision, give the one-line WHY inline and point to the DR.>

## Components

- **<component>** — <what it does + the key constants/IDs (see `CONSTANTS.md`) + the
  one-line WHY where it isn't obvious>.

## What <PROJECT> IS / is NOT

- IS: <the load-bearing positive constraints>
- is NOT: <the rejected directions, kept as durable constraints — these prevent a
  rebuilder re-adding scope you deliberately excluded>

## Key decisions at a glance

| Decision | Choice | Why (one line) | Full reasoning |
| --- | --- | --- | --- |
| <topic> | <choice> | <rationale> | DR-NNNN / design-log <date> |

---

*Role: SOURCE. Hand-maintained. See `AGENTS.md` §1 for the doc map.*
